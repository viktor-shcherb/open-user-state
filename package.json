{
  "name": "open-user-state",
  "version": "1.0.0",
  "description": "Cloudflare Worker for GitHub OAuth and repo state management",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler build",
    "publish": "wrangler publish",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20230517.0"
  }
}
